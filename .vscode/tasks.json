{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build riscv simulation",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/sim/ghdl/riscv"
            },
            "command": "hdlmake"
        },
        {
            "label": "run riscv simulation",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/sim/ghdl/riscv"
            },
            "command": "make"
        }
    ]
}